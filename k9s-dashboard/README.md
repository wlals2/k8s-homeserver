# K9s - Kubernetes CLI

터미널 기반 Kubernetes 클러스터 관리 도구

## 🚀 설치

```bash
cd ~/project/k8s/k9s
chmod +x install-k9s.sh
./install-k9s.sh
```

## 📖 사용법

### 기본 시작
```bash
k9s
```

### 특정 네임스페이스로 시작
```bash
k9s -n monitoring
k9s -n nextcloud
k9s -n kube-system
```

### 모든 네임스페이스 보기
```bash
k9s -A
```

## ⌨️ 주요 단축키

### 네비게이션
| 키 | 설명 |
|----|------|
| `:pods` | Pod 목록 보기 |
| `:svc` | Service 목록 |
| `:deploy` | Deployment 목록 |
| `:ns` | Namespace 목록 |
| `:pvc` | PersistentVolumeClaim 목록 |
| `:nodes` | Node 목록 |
| `0` | 모든 네임스페이스 보기 |
| `↑` `↓` | 항목 선택 |

### 작업
| 키 | 설명 |
|----|------|
| `l` | 로그 보기 |
| `s` | 쉘 접속 (exec) |
| `d` | Describe (상세 정보) |
| `y` | YAML 보기 |
| `e` | 편집 |
| `ctrl-d` | 삭제 |
| `ctrl-k` | Kill (강제 종료) |

### 검색 및 필터
| 키 | 설명 |
|----|------|
| `/` | 검색 모드 |
| `/pattern` | 패턴으로 필터링 |
| `esc` | 검색 취소 |

### 기타
| 키 | 설명 |
|----|------|
| `?` | 도움말 |
| `q` | 뒤로가기/종료 |
| `ctrl-a` | 별칭 보기 |
| `ctrl-r` | 새로고침 |

## 🎯 실전 예제

### 예제 1: Pod 로그 확인
```bash
1. k9s 실행
2. :pods 입력
3. /nextcloud 입력 (검색)
4. Enter로 선택
5. l 키 눌러서 로그 보기
```

### 예제 2: Pod 쉘 접속
```bash
1. k9s 실행
2. :pods 입력
3. 원하는 Pod 선택
4. s 키 눌러서 쉘 접속
5. exit로 나가기
```

### 예제 3: Deployment 스케일링
```bash
1. k9s 실행
2. :deploy 입력
3. 원하는 Deployment 선택
4. s 키로 스케일 조정
```

### 예제 4: 네임스페이스 전환
```bash
1. k9s 실행
2. :ns 입력
3. 네임스페이스 선택 (Enter)
```

### 예제 5: 리소스 사용량 확인
```bash
1. k9s 실행
2. :nodes 입력
3. CPU/메모리 사용량 실시간 확인
```

## 🔧 설정

### 설정 파일 위치
```
~/.config/k9s/config.yml
```

### 주요 설정 항목
```yaml
k9s:
  # 리프레시 간격 (초)
  refreshRate: 2
  
  # 로그 설정
  logger:
    tail: 100        # 로그 표시 라인 수
    buffer: 5000     # 버퍼 크기
    sinceSeconds: 60 # 최근 n초 로그
```

### 테마 변경
```bash
# 사용 가능한 스킨 목록
ls ~/.config/k9s/skins/

# config.yml에서 스킨 변경
k9s:
  ui:
    skin: nord  # 또는 monokai, dracula 등
```

## 💡 유용한 팁

### Tip 1: 별칭 설정
```bash
# ~/.bashrc에 추가
echo 'alias k="k9s"' >> ~/.bashrc
source ~/.bashrc

# 이제 k만 입력해도 실행
k
```

### Tip 2: 컨텍스트 전환
```bash
# 여러 클러스터 사용 시
k9s --context my-cluster
```

### Tip 3: 읽기 전용 모드
```bash
# 실수로 삭제 방지
k9s --readonly
```

### Tip 4: 로그 필터링
```bash
# Pod 로그에서 특정 키워드 검색
# l 키로 로그 진입 → / 키로 검색
/error
/warning
```

### Tip 5: 빠른 네임스페이스 이동
```bash
# monitoring 네임스페이스의 Pod 바로 보기
k9s -n monitoring

# 실행 후에도 네임스페이스 전환
:ns → 선택 → Enter
```

## 🎨 화면 구성

```
┌─────────────────────────────────────────────────────────┐
│ Context: kubernetes-admin@kubernetes                    │
│ Cluster: kubernetes              Namespace: monitoring  │
├─────────────────────────────────────────────────────────┤
│ NAME                    READY   STATUS    RESTARTS  AGE │
│ prometheus-xxx          1/1     Running   0         2d  │
│ grafana-xxx             1/1     Running   0         2d  │
│ node-exporter-xxx       1/1     Running   0         2d  │
├─────────────────────────────────────────────────────────┤
│ <pods> [l]ogs [d]escribe [s]hell [y]aml              ? │
└─────────────────────────────────────────────────────────┘
```

## 🔍 트러블슈팅

### kubectl이 작동하지 않으면 K9s도 작동 안 함
```bash
# kubeconfig 확인
echo $KUBECONFIG

# kubectl 테스트
kubectl get nodes
```

### 권한 문제
```bash
# kubeconfig 파일 권한 확인
chmod 600 ~/.kube/config
```

### 느린 반응
```bash
# config.yml에서 refreshRate 조정
refreshRate: 5  # 2초 → 5초
```

## 🆚 K9s vs kubectl 명령어 비교

| 작업 | kubectl | K9s |
|------|---------|-----|
| Pod 목록 | `kubectl get pods` | `:pods` |
| 로그 보기 | `kubectl logs pod-name` | `l` |
| Pod 삭제 | `kubectl delete pod pod-name` | `ctrl-d` |
| Describe | `kubectl describe pod pod-name` | `d` |
| YAML 보기 | `kubectl get pod pod-name -o yaml` | `y` |
| 쉘 접속 | `kubectl exec -it pod-name -- sh` | `s` |
| 네임스페이스 전환 | `-n namespace` | `:ns` → 선택 |

## 📚 추가 리소스

- [K9s 공식 GitHub](https://github.com/derailed/k9s)
- [K9s 단축키 치트시트](https://k9scli.io/topics/commands/)
- [K9s 스킨 갤러리](https://github.com/derailed/k9s/tree/master/skins)

## 🔗 관련 도구

K9s와 함께 사용하면 좋은 도구들:
- **kubectl** - 기본 CLI
- **kubectx/kubens** - 컨텍스트/네임스페이스 빠른 전환
- **stern** - 여러 Pod 로그 동시 보기
- **kube-ps1** - 프롬프트에 K8s 컨텍스트 표시

## ⚡ 퀵 스타트 체크리스트

- [ ] K9s 설치 완료
- [ ] `k9s` 명령어로 실행 확인
- [ ] `:pods` 로 Pod 목록 보기
- [ ] `l` 키로 로그 확인
- [ ] `?` 키로 도움말 확인
- [ ] `:ns` 로 네임스페이스 전환
- [ ] `q` 키로 종료

이제 K9s로 Kubernetes 클러스터를 관리할 준비가 되었습니다! 🎉